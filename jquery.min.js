
// Code to refresh page if page size changed by threshold in order to prevent erroneous layout on page resizing
var w = $(window).width();
var threshold = 100
$(window).resize(function () {
  widthDiff = Math.abs($(window).width() - w);
  w = $(window).width();
  if (widthDiff < threshold) return;
    location.reload();

})
